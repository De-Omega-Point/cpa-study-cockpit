export function calculateReadiness(input:{lessons:number;totalLessons:number;accuracy:number;cases:number;vocab:number;mock:number}){ const lesson=input.totalLessons?input.lessons/input.totalLessons*30:0; const acc=input.accuracy*0.35; const cases=Math.min(input.cases,10)/10*15; const vocab=Math.min(input.vocab,100)/100*10; const mock=Math.min(input.mock,4)/4*10; return Math.round(Math.min(100, lesson+acc+cases+vocab+mock)); }
